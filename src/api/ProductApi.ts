import _ from 'lodash';
import Product from '../models/product';
import config from '../constants/config';

const { domain } = config;

export const getHottestProducts = async (count: number = 20) => {};
